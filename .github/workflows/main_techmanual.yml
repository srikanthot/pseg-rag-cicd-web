# ⚠️  DEPRECATED — do not use this workflow
#
# This file was auto-generated by the Azure Portal Deployment Center.
# It has been superseded by deploy.yml which supports multi-environment
# (dev | test | prod) deployments with proper secrets management.
#
# To prevent this file from running alongside deploy.yml it has been
# changed to workflow_dispatch only (no push trigger).
# You can safely DELETE this file from your repository.

name: "[DEPRECATED] Azure Portal – techmanual"

on:
  workflow_dispatch:    # manual only — kept for historical reference

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: "[DEPRECATED] Use deploy.yml instead"
        run: |
          echo "This workflow is deprecated."
          echo "Use the 'RAG Chatbot – Deploy to Azure' workflow (deploy.yml) instead."
          exit 1
